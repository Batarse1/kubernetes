---
apiVersion: v1
kind: Service
metadata:
  name: dictionaryapp-service
  labels:
    app: mysql
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 80
    nodePort: 30004
    protocol: TCP
  selector:
    app: mysql4